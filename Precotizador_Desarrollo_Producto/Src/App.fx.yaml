App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Set(SelectedID , "dsa")
        set

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

