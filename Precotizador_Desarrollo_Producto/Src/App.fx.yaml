App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Set(SelectedID , "dsa");
        Set(VarTarjetaVisible,true);
        Set(VarRejillaVisible,false)
    StartScreen: =

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

