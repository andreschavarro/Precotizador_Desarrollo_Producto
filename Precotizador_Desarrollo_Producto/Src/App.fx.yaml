App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(SelectedID , "dsa");
        Set(JSONData, "");
        Set(VarTarjetaVisible,true);
        Set(VarRejillaVisible,false)

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

